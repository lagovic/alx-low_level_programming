#include <stdio.h>

/**
* main - Prints numbers numbers 0 to 9
*
* Return: Alwaysa 0 (Success)
*/

int main(void)
{

	int num;

	for (num = 0; num < 10; num++)
{
printf("%d", num);
}
printf("\n");
return (0);
}
